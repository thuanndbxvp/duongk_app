# Bối cảnh (CONTEXT): Task 1.2 - Thiết lập Infrastructure (Docker & Observability)

Mục đích: Thiết lập file docker-compose.yml với 4 worker pools (ML, High, IO, Normal) để tránh nghẽn cổ chai. Cấu hình .env.example, setup Sentry và Logging.